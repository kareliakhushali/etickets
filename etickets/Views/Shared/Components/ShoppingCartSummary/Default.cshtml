@model int;
@* here we are writing int bcz in invoke method in Shopping Cart summary file we have returned count which will be under int
*@ 
@if(Model > 0)
            {
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4"></i>@Model
        </a>
    </li>

            }